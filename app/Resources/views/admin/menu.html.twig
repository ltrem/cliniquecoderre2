				
<div id="navbar" class="collapse navbar-collapse">
    <ul class="nav navbar-nav">
		{% if is_granted('ROLE_SUPER_ADMIN') %}
			<li><a href="{{ path('admin_employe') }}">Employés</a></li>
		{% endif %}
		<li><a href="{{ path('admin_client') }}">Clients</a></li>
		<li><a href="{{ path('admin_event') }}">Rendez-vous</a></li>
    </ul>
    <ul class="nav navbar-nav navbar-right">

		  <li class="dropdown">

			    {% if is_granted('IS_AUTHENTICATED_FULLY') %}

                    <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">
			    		Mon profil <span class="caret"></span>
			    	</a>

				    <ul class="dropdown-menu">
						<li><a href="{{ path('admin_profile') }}">Mon profile</a></li> {# TODO: Ajuster le profile #}
						<li role="separator" class="divider"></li>
						<li class="dropdown-header">Connexion</li>
			    		<li><a href="{{ path('security_logout') }}">Se déconnecter</a></li>

				    </ul>

		    	{% else %}

		    		<a href="{{ path('security_login') }}">Login</a>

			    {% endif %}

		  </li>

    </ul>
</div><!--/.nav-collapse -->