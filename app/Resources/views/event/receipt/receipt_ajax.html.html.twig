
{{ form_start(form, {'attr': {'id': 'receipt_new'}, 'action': path('admin_receipt_new', { 'id': event.id })}) }}
<div class="modal-header">
    <button type="button" class="close" data-dismiss="modal" aria-label="Close"><span aria-hidden="true">&times;</span></button>
    <h4 class="modal-title" id="exampleModalLabel">{{ 'event.receipt.title'|trans }}</h4>
</div>
<div class="modal-body">
    {{ form_widget(form) }}
</div>
<div class="modal-footer">
    <button type="button" class="btn btn-default" data-dismiss="modal">{{ 'general.modal.close'|trans }}</button>
    <button type="submit" class="btn btn-success" data-loading-text="Sauvegarde du reçus en cours...">{{ 'event.receipt.submit'|trans }}</button>
</div>
{{ form_end(form) }}


<script>


    $(".event_datetimepicker").datetimepicker({
        bootcssVer: 3,
        format: 'yyyy-mm-dd hh:ii',
        autoclose: true,
        stepping: 60,
        minView: 'day',
        todayHighlight: true,
        language: 'fr',
        todayBtn: true
        {#pickerPosition: "bottom-left",#}
    })

    $("#receipt_new").submit(function(e){

        e.preventDefault();
        $(this).find(':submit').button('loading');
        var formSerialize = $(this).serialize();
        $.post($(this).attr('action'), formSerialize, function(response){
            //your callback here
            location.reload();

        },'JSON');
    });
</script>