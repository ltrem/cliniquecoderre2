{% extends "base.html.twig" %}

{% block body %}

    <h1>Page d'accueil FROM GIT</h1>

    {% if is_granted('IS_AUTHENTICATED_FULLY') %}

        <p>
            <a href="{{ path('security_logout') }}">Logout</a></p>

    {% else %}

        <p>
            <a href="{{ path('security_login') }}">Login</a></p>

    {% endif %}


{% endblock %}